# macOS
.DS_Store

# Logs
logs/
*.log

# Packet captures - may contain real facility IPs/MACs/hostnames
*.pcap
*.pcapng
/captures

# Data dir, if Demeter is run with --data-dir inside the repo
/DemeterData/

# Go build outputs
/demeter
/demeter-desktop
/demeter-linux
/demeter*.exe
/demeter-v*
/Demeter.app/
/Demeter-v*
/rcprobe
/rcprobe*

# Legacy Electron/JS build artifacts (the app's source lives on the `legacy`
# branch). Ignored so a stray `npm install` / build from a legacy checkout can't
# be staged on main.
node_modules/
dist/
main.js
